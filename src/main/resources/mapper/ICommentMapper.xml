<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="iwishing.ccCommunity.community.mapper.ICommentMapper">
    <insert id="insertComent" parameterType="comment">
        insert into comment (parent_id, type, content, gmt_create, gmt_modified, commentator)
        VALUES (#{parent_id},#{type},#{content},#{gmtCreate},#{gmtModified},#{commentator});
    </insert>
</mapper>